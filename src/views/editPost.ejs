<%- include("partials/head")%>

<body>
  <%- include("partials/navbar")%> <% if(result){ %>
  <div class="card">
    <h2>Edit Post</h2>
    <form action="/updatePost" method="post">
      <input name="id" value="<%= result.id %>" hidden />
      <div class="form-group">
        <label for="posteo">Título:</label>
        <input
          type="text"
          id="titulo"
          name="titulo"
          value="<%= result.titulo %>"
          required
        />
      </div>
      <div class="form-group">
        <label for="posteo">Contenido:</label>
        <textarea id="contenido" name="contenido" rows="5" required>
<%= result.contenido %>
        </textarea>
      </div>
      <div class="form-group">
        <label for="posteo">URL Imagen:</label>
        <input
          type="text"
          id="urlImagen"
          name="urlImagen"
          value="<%= result.urlImagen %>"
          required
        />
      </div>
      <div class="btn-group">
        <button type="submit" class="btn btn-primary">Edit</button>
        <a href="/post" class="btn btn-secondary">Cancelar</a>
      </div>
    </form>
  </div>
  <% }%>
</body>
<%- include("partials/footer")%>
